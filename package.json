{
  "name": "array-scope",
  "displayName": "ArrayScope Scientific Image Viewer",
  "description": "Read-only, progressive scientific image viewer for NPY and TIFF files.",
  "version": "0.1.0",
  "publisher": "AmidStars",
  "icon": "images/icon.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mdw771/ArrayScope.git"
  },
  "homepage": "https://github.com/mdw771/ArrayScope#readme",
  "bugs": {
    "url": "https://github.com/mdw771/ArrayScope/issues"
  },
  "engines": {
    "vscode": "^1.100.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "scientific",
    "image",
    "numpy",
    "npy",
    "tiff"
  ],
  "extensionKind": [
    "workspace"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onCustomEditor:scientificImageViewer.viewer"
  ],
  "files": [
    "dist",
    "images",
    "README.md",
    "CHANGELOG.md",
    "LICENSE",
    "THIRD_PARTY_NOTICES.md"
  ],
  "scripts": {
    "clean": "rm -rf dist coverage *.vsix",
    "build": "node esbuild.mjs",
    "build:integration": "tsc -p tsconfig.integration.json",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "npm run build && npm run build:integration && node dist/test/runTest.js",
    "test:watch": "vitest",
    "package": "npm run clean && npm run typecheck && npm test && npm run build && vsce package --no-dependencies --allow-missing-repository"
  },
  "contributes": {
    "customEditors": [
      {
        "viewType": "scientificImageViewer.viewer",
        "displayName": "ArrayScope Scientific Image Viewer",
        "selector": [
          { "filenamePattern": "*.npy" },
          { "filenamePattern": "*.tif" },
          { "filenamePattern": "*.tiff" }
        ],
        "priority": "default"
      }
    ],
    "commands": [
      { "command": "scientificImageViewer.tool.rectangle", "title": "ArrayScope Scientific Image Viewer: Rectangle Selection" },
      { "command": "scientificImageViewer.tool.ellipse", "title": "ArrayScope Scientific Image Viewer: Ellipse Selection" },
      { "command": "scientificImageViewer.tool.line", "title": "ArrayScope Scientific Image Viewer: Line Selection" },
      { "command": "scientificImageViewer.tool.polygon", "title": "ArrayScope Scientific Image Viewer: Polygon Selection" },
      { "command": "scientificImageViewer.tool.sampler", "title": "ArrayScope Scientific Image Viewer: Sampler" },
      { "command": "scientificImageViewer.computeStatistics", "title": "ArrayScope Scientific Image Viewer: Calculate Statistics" },
      { "command": "scientificImageViewer.plotLineProfile", "title": "ArrayScope Scientific Image Viewer: Plot Line Profile" },
      { "command": "scientificImageViewer.tool.magnifier", "title": "ArrayScope Scientific Image Viewer: Magnifier" },
      { "command": "scientificImageViewer.tool.pan", "title": "ArrayScope Scientific Image Viewer: Pan" },
      { "command": "scientificImageViewer.autoContrast", "title": "ArrayScope Scientific Image Viewer: Auto Contrast" },
      { "command": "scientificImageViewer.clearSelection", "title": "ArrayScope Scientific Image Viewer: Clear Selection" },
      { "command": "scientificImageViewer.fitToWindow", "title": "ArrayScope Scientific Image Viewer: Fit to Window" },
      { "command": "scientificImageViewer.actualPixels", "title": "ArrayScope Scientific Image Viewer: Actual Pixels" },
      { "command": "scientificImageViewer.zoomIn", "title": "ArrayScope Scientific Image Viewer: Zoom In" },
      { "command": "scientificImageViewer.zoomOut", "title": "ArrayScope Scientific Image Viewer: Zoom Out" },
      { "command": "scientificImageViewer.nextSlice", "title": "ArrayScope Scientific Image Viewer: Next Slice" },
      { "command": "scientificImageViewer.previousSlice", "title": "ArrayScope Scientific Image Viewer: Previous Slice" }
    ],
    "keybindings": [
      { "command": "scientificImageViewer.tool.rectangle", "key": "r", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.tool.ellipse", "key": "e", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.tool.line", "key": "l", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.tool.polygon", "key": "p", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.tool.sampler", "key": "i", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.computeStatistics", "key": "ctrl+t", "mac": "cmd+t", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.plotLineProfile", "key": "ctrl+k", "mac": "cmd+k", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.tool.magnifier", "key": "z", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.zoomOut", "key": "-", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.zoomIn", "key": "=", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.tool.pan", "key": "h", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.clearSelection", "key": "ctrl+alt+backspace", "mac": "cmd+alt+backspace", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.fitToWindow", "key": "f", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.actualPixels", "key": "1", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.nextSlice", "key": "]", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" },
      { "command": "scientificImageViewer.previousSlice", "key": "[", "when": "activeCustomEditorId == scientificImageViewer.viewer && !inputFocus && !terminalFocus" }
    ],
    "configuration": {
      "title": "ArrayScope Scientific Image Viewer",
      "properties": {
        "scientificImageViewer.localCacheMB": {
          "type": "number",
          "default": 256,
          "minimum": 32,
          "description": "Maximum numeric tile cache size in each local webview, in MiB."
        },
        "scientificImageViewer.remoteCacheMB": {
          "type": "number",
          "default": 512,
          "minimum": 32,
          "description": "Maximum decoded tile cache size on the extension host, in MiB."
        },
        "scientificImageViewer.tileSize": {
          "type": "number",
          "default": 256,
          "enum": [128, 256, 512],
          "description": "Tile edge length in pixels."
        },
        "scientificImageViewer.automaticHistogramPixelLimit": {
          "type": "integer",
          "default": 1000000,
          "minimum": 0,
          "description": "Maximum number of pixels in the current slice or selection for automatic histogram calculation. Larger scopes require clicking Recalculate."
        }
      }
    }
  },
  "dependencies": {
    "geotiff": "^3.0.5",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/vscode": "^1.100.0",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.1",
    "typescript": "^5.8.0",
    "vitest": "^4.1.10"
  }
}
